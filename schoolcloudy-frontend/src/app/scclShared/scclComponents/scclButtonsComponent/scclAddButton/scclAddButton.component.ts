import { Component, Input, EventEmitter, Output } from '@angular/core';

@Component({
    selector: 'sccl-add-button',
    template: `
            
    `,

})
export class ScclAddButtonComponent {
   
}
