import { Component } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';

@Component({
    selector: 'sccl-timetables',
    template: `
        <h3> Tabs that show school calendar, school timetable, examination timetable etc</h3>
    `
})
export class ScclTimetablesComponent {

    constructor() {
    }
}
